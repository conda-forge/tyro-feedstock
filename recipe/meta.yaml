{% set name = "tyro" %}
{% set version = "0.5.18" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/tyro-{{ version }}.tar.gz
  sha256: 26c07c87f2a00a18a7b8169b0338f086665b7a96a8e8eef237aafc1158a5fb23

build:
  noarch: python
  script: {{ PYTHON }} -m pip install --no-deps . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - setuptools >=61.0
    - pip
  run:
    - python >=3.7
    - docstring_parser >=0.14.1
    - typing-extensions >=4.3.0
    - pyyaml >=6.0
    - colorama >=0.4.0
    - frozendict >=2.3.4
    - rich >=11.1.0
    - shtab >=1.5.6

test:
  imports:
    - tyro
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/brentyi/tyro
  summary: Strongly typed, zero-effort CLI interfaces
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - traversaro
